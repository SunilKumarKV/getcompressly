# Contributing

1. Create a branch from `develop`.
2. Keep changes scoped and typed.
3. Run `pnpm lint` and `pnpm build`.
4. Include database migrations for Prisma schema changes.
5. Open a pull request using the template.
